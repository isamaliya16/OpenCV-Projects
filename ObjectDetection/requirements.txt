# Runtime dependencies. Upper bounds keep a future major release from breaking the app.
Flask>=3.1,<4
ultralytics>=8.4,<9        # 8.4 is the first release with YOLO26; installs PyTorch too
opencv-python>=4.10,<5     # OpenCV 5 changes the DNN importer - not tested with YuNet yet
numpy>=1.26
Pillow>=10                 # used to check image dimensions before decoding
